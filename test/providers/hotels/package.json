{
  "name": "xtravels-hotels-provider",
  "version": "0.1.0",
  "private": true,
  "description": "Serves this app's bundled HotelsService over OData, so the federation tests (and the demo) can delegate to it as a real remote.",
  "scripts": {
    "start": "cds mock sap.capire.hotels.HotelsService"
  },
  "dependencies": {
    "@sap/cds": ">=9"
  },
  "devDependencies": {
    "@cap-js/sqlite": ">=2"
  }
}
